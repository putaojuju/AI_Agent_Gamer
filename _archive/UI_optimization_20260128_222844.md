## 执行计划：UI 极简优化与窗口拖拽重构

### 目标
- 移除 `DraggableWindow` 类中的顶部标题栏（标题文字、关闭按钮、透明度切换按钮）
- 保留"实时视觉 (Live Vision)"控制条和画面显示区域
- 实现点击窗口背景即可拖拽窗口

### 修改范围
- `main.py` 中的 `DraggableWindow` 类

### 详细步骤

#### 步骤 A: 修改 `create_widgets` 方法
1. 删除 `self.title_bar` 及其子控件（`title_label`, `transparency_btn`, `close_btn`）
2. 调整 `self.content_frame` 布局，使其填满整个 Frame
3. 保留 `self.resize_handle`（右下角缩放柄）

#### 步骤 B: 重构拖拽事件绑定
- 将 `<Button-1>` 和 `<B1-Motion>` 事件从 `title_bar` 转移到 `self` 和 `self.content_frame`
- 确保按住窗口空白处可拖拽

#### 步骤 C: 样式微调
- 在 `__init__` 中设置 `border_width=1`, `border_color="#555555"` 增强视觉边界

#### 步骤 D: 清理无用代码
- 删除 `toggle_transparency` 方法

### 预期效果
- 窗口更简洁，无顶部标题栏
- 可通过按住窗口任意空白处拖拽
- 保留缩放功能和内容显示